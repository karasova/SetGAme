package com.example.setgame;

public class Card {
    int fill, count, shape, color;

    @Override
    public String toString() {
        return "Card{" + "fill=" + fill +", count=" + count +", shape=" + shape +", color=" + color + '}';
    }
}
